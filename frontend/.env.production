# Public Render origin. Safe to commit — this is baked into the browser bundle.
# Not a secret. Vercel "Sensitive" env vars cannot be converted to Config; delete
# the dashboard Secret so it does not override this file at build time.
VITE_API_BASE_URL=https://trip-planner-2-3t88-n3kx.onrender.com
