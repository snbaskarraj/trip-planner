# Locally: leave unset so Vite proxies /api to http://127.0.0.1:8000.
# On Vercel: set this to the Render origin (no trailing slash) and redeploy.
# Example: https://trip-planner-2-3t88.onrender.com
VITE_API_BASE_URL=
